// src/modules/calculator/calculator.service.ts
export class CalculatorService {
    public multiply(a: number, b: number): number {
        // Чистая бизнес-логика, ничего лишнего
        return a * b;
    }
}