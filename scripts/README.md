# 📚 ИНСТРУКЦИЯ ПО ЗАГРУЗКЕ ДАННЫХ ПОЛЬЗОВАТЕЛЕЙ

## 🎯 Назначение

Скрипт `upload-user-data.ts` позволяет загружать или обновлять данные пользователей в базе данных.

---

## 🚀 Как использовать

### Шаг 1: Откройте файл скрипта

```bash
code scripts/upload-user-data.ts
```

### Шаг 2: Отредактируйте массив `USERS_TO_UPLOAD`

Найдите секцию с конфигурацией пользователей (примерно строка 60):

```typescript
const USERS_TO_UPLOAD: UserData[] = [
    {
        telegramId: 334987233,  // ← Ваш Telegram ID
        nickname: 'YourName',   // ← Ваш никнейм

        // Binance Mainnet
        binanceApiKey: 'YOUR_KEY',
        binanceApiSecret: 'YOUR_SECRET',

        // ... остальные ключи
    },
    // Добавьте других пользователей здесь
];
```

### Шаг 3: Запустите скрипт

```bash
npx ts-node scripts/upload-user-data.ts
```

---

## 📋 Примеры использования

### Пример 1: Добавить одного пользователя со всеми ключами

```typescript
const USERS_TO_UPLOAD: UserData[] = [
    {
        telegramId: 123456789,
        nickname: 'John',

        // Mainnet
        binanceApiKey: 'abc123...',
        binanceApiSecret: 'xyz789...',
        hlPrivateKey: '0x...',
        hlWalletAddress: '0x...',
        
        // Testnet
        binanceApiKeyTest: 'test_abc...',
        binanceApiSecretTest: 'test_xyz...',
    },
];
```

### Пример 2: Добавить несколько пользователей

```typescript
const USERS_TO_UPLOAD: UserData[] = [
    {
        telegramId: 111111111,
        nickname: 'User1',
        binanceApiKey: 'key1',
        binanceApiSecret: 'secret1',
    },
    {
        telegramId: 222222222,
        nickname: 'User2',
        binanceApiKey: 'key2',
        binanceApiSecret: 'secret2',
    },
    {
        telegramId: 333333333,
        nickname: 'User3',
        hlPrivateKey: 'hl_key3',
        hlWalletAddress: '0x...',
    },
];
```

### Пример 3: Обновить существующего пользователя

Если пользователь с `telegramId: 123456789` уже есть в БД:

```typescript
const USERS_TO_UPLOAD: UserData[] = [
    {
        telegramId: 123456789,  // Существующий пользователь
        nickname: 'UpdatedName', // Обновим никнейм
        
        // Добавим новые ключи
        paradexAccountAddress: '0xnew...',
        paradexPrivateKey: 'new_key...',
    },
];
```

**Результат:** Данные пользователя будут **обновлены** (не удалены и созданы заново).

---

## 🔐 Безопасность

### Автоматическое шифрование

Следующие поля **автоматически шифруются** перед сохранением:

- `binanceApiKey`, `binanceApiSecret`
- `paradexPrivateKey`
- `lighterPrivateKey`
- `extendedApiKey`, `extendedStarkPrivateKey`
- `hlPrivateKey`
- Все testnet-версии этих ключей

### Требования к ENCRYPTION_KEY

В файле `.env` должен быть настроен `ENCRYPTION_KEY`:

```env
ENCRYPTION_KEY=your_32_character_secret_key_here
```

**ВАЖНО:** Длина ключа должна быть **ровно 32 символа**!

---

## 📊 Что показывает скрипт

При запуске вы увидите:

```
╔════════════════════════════════════════════════════════════╗
║  🔐 СКРИПТ ЗАГРУЗКИ ДАННЫХ ПОЛЬЗОВАТЕЛЕЙ В БД             ║
╚════════════════════════════════════════════════════════════╝

📋 Всего пользователей для загрузки: 1

🔐 ENCRYPTION_KEY найден и валиден

📤 Обработка пользователя: YourNickname
   ℹ️  Пользователь существует (ID: 1)
   🔄 Обновление данных...
   ✅ Данные успешно обновлены
   📊 Настроенные биржи:
      Binance:     ✅
      Hyperliquid: ✅
      Paradex:     ✅
      Lighter:     ✅
      Extended:    ✅

════════════════════════════════════════════════════════════
📊 ИТОГОВАЯ СТАТИСТИКА:
════════════════════════════════════════════════════════════
✅ Успешно обработано: 1
❌ Ошибок:             0
📝 Всего:              1
════════════════════════════════════════════════════════════

🎉 Все пользователи успешно загружены!

👋 Отключение от базы данных...
✅ Готово!
```

---

## 🔍 Проверка результата

### Через Prisma Studio

```bash
npx prisma studio
```

Откройте таблицу `User` и найдите своего пользователя по `telegramId`.

### Через SQL

```sql
SELECT "telegramId", "nickname", 
       CASE WHEN "binanceApiKey" IS NOT NULL THEN '✅' ELSE '❌' END as binance,
       CASE WHEN "hlPrivateKey" IS NOT NULL THEN '✅' ELSE '❌' END as hyperliquid
FROM "User";
```

---

## ⚠️ Важные замечания

### 1. Обновление vs Создание

- Если пользователь с таким `telegramId` **существует** → данные **обновляются**
- Если пользователь **не существует** → создается **новый**

### 2. Частичное обновление

Вы можете обновить только некоторые поля:

```typescript
{
    telegramId: 123456789,
    // Обновим только nickname, остальные поля не тронем
    nickname: 'NewNickname',
}
```

### 3. Пустые значения

Поля со значением `null` или `undefined` **не перезапишут** существующие данные.

### 4. Безопасность ключей

**НИКОГДА** не коммитьте этот файл с реальными ключами в Git!

Добавьте в `.gitignore`:
```
scripts/upload-user-data.ts
```

Или храните шаблон отдельно:
```
scripts/upload-user-data.template.ts  ← Шаблон (в Git)
scripts/upload-user-data.ts           ← Рабочий файл (не в Git)
```

---

## 🐛 Решение проблем

### Ошибка: "ENCRYPTION_KEY не настроен"

**Решение:**
```bash
# Проверьте .env файл
cat .env | grep ENCRYPTION_KEY

# Должно быть:
ENCRYPTION_KEY=your_32_character_key_here_123
```

### Ошибка: "telegramId is required"

**Решение:** Убедитесь, что у каждого пользователя указан `telegramId`:
```typescript
{
    telegramId: 123456789, // ← Обязательно!
    nickname: 'User',
}
```

### Ошибка подключения к БД

**Решение:** Проверьте `DATABASE_URL` в `.env`:
```bash
cat .env | grep DATABASE_URL
```

---

## 📝 Полный пример рабочего файла

```typescript
const USERS_TO_UPLOAD: UserData[] = [
    // Администратор
    {
        telegramId: 334987233,
        nickname: 'Admin',
        
        // Все биржи Mainnet
        binanceApiKey: 'mainnet_key',
        binanceApiSecret: 'mainnet_secret',
        hlPrivateKey: '0xmainnet...',
        hlWalletAddress: '0xwallet...',
        paradexAccountAddress: '0xparadex...',
        paradexPrivateKey: '0xparadex_key...',
        
        // Все биржи Testnet
        binanceApiKeyTest: 'testnet_key',
        binanceApiSecretTest: 'testnet_secret',
        hlTestPrivateKey: '0xtestnet...',
        hlTestWalletAddress: '0xtest_wallet...',
    },
    
    // Тестовый пользователь (только Binance Testnet)
    {
        telegramId: 987654321,
        nickname: 'TestUser',
        binanceApiKeyTest: 'test_key',
        binanceApiSecretTest: 'test_secret',
    },
];
```

---

## 🎓 Как получить Telegram ID

### Способ 1: Через бота

Напишите боту `/start` и посмотрите в логах сервера:
```
User ID: 123456789
```

### Способ 2: Через @userinfobot

1. Найдите в Telegram: `@userinfobot`
2. Отправьте `/start`
3. Бот покажет ваш ID

### Способ 3: Через код

Добавьте в любой обработчик бота:
```typescript
console.log('User ID:', ctx.from?.id);
```

---

## 🔄 Регулярное использование

### Рекомендуемый workflow:

1. **Создайте шаблон:**
   ```bash
   cp scripts/upload-user-data.ts scripts/upload-user-data.template.ts
   ```

2. **Очистите ключи в шаблоне:**
   ```typescript
   // В template файле замените реальные ключи на плейсхолдеры
   binanceApiKey: 'YOUR_BINANCE_API_KEY',
   ```

3. **Добавьте в .gitignore:**
   ```
   scripts/upload-user-data.ts
   ```

4. **При добавлении пользователя:**
   ```bash
   # Скопируйте шаблон
   cp scripts/upload-user-data.template.ts scripts/upload-user-data.ts
   
   # Отредактируйте
   code scripts/upload-user-data.ts
   
   # Запустите
   npx ts-node scripts/upload-user-data.ts
   ```

---

## ✅ Чеклист перед запуском

- [ ] `ENCRYPTION_KEY` настроен в `.env` (32 символа)
- [ ] `DATABASE_URL` настроен в `.env`
- [ ] Все `telegramId` указаны корректно
- [ ] Реальные API ключи заменены на ваши
- [ ] Файл НЕ будет закоммичен в Git
- [ ] База данных доступна

---

**Готово!** Теперь вы можете безопасно управлять пользователями через этот скрипт.
